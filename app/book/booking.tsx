'use client';
import {useState} from 'react';
import {PawPrint,CheckCircle} from 'lucide-react';
import {Field,today} from '../forms';
export default function Booking(){const[busy,setBusy]=useState(false),[error,setError]=useState(''),[done,setDone]=useState(false);return <main className="booking"><a className="brand" href="/"><PawPrint/>The Animal Place</a>{done?<section className="panel"><CheckCircle size={36}/><h1>Request received.</h1><p>Your preferred visit has been saved. It is a request until the clinic confirms it.</p><a className="button" href="/">Back to the website</a></section>:<><h1>A little care starts here.</h1><p className="muted">Tell us who’s visiting and your preferred time. The clinic will review your request before confirming.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const f=Object.fromEntries(new FormData(e.currentTarget));try{const r=await fetch('/api/records',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({kind:'appointments',data:{...f,status:'Requested'}})});const j:any=await r.json();if(!r.ok)throw Error(j.error);setDone(true)}catch(e:any){setError(e.message)}finally{setBusy(false)}}}><div className="form-grid"><Field name="pet" label="Pet’s name"/><Field name="owner" label="Your name"/><Field name="phone" label="Phone number" type="tel"/><Field name="reason" label="Reason for visit"/><Field name="date" label="Preferred date" type="date" min={today()}/><Field name="time" label="Preferred time (IST)" type="time"/></div>{error&&<p className="error" role="alert">{error}</p>}<div className="form-actions"><button className="button" disabled={busy}>{busy?'Saving…':'Request appointment ↗'}</button></div></form></>}</main>}

